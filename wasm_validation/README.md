Library to generate a wasm file to be used from JavaScript environment
such as browsers.

This folder has been setup using

```
cargo generate --git https://github.com/rustwasm/wasm-pack-template.git --name validate
```